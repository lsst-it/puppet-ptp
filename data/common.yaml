---
ptp::packages:
   - linuxptp
ptp::services:
   chronyd:
      ensure: stopped
      enable: false
   ntpd:
      ensure: stopped
      enable: false
   ptp4l:
      ensure: running
      enable: true
   phc2sys:
      ensure: running
      enable: true

